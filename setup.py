#!/usr/bin/python3

from distutils.core import setup

setup(
    name='radio-to-elf',
    version='1.0',
    description='A tool to recover a fully analyzable ELF file from a raw Shannon baseband radio or modem file',
    # TODO add those
    author='',
    author_email='',
    utl='',
    install_requires=[],
    packages=['radio_to_elf'],
    scripts=['radio-to-elf']
)
